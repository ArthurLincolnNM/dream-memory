{
  "name": "dream-memory",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pi extension for persistent memory with hybrid search, contradiction detection, and dream consolidation",
  "scripts": {
    "test": "node --import tsx --test tests/*.test.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@mariozechner/pi-ai": "*",
    "@mariozechner/pi-coding-agent": "*",
    "better-sqlite3": "*",
    "typebox": "*"
  },
  "optionalDependencies": {
    "@huggingface/transformers": "^3.8.1"
  },
  "devDependencies": {
    "tsx": "^4.22.4",
    "typescript": "^5.6.0"
  }
}
